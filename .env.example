# Required for PII proxy (sanitize + heavy LLM + unmask)
OPENAI_API_KEY=sk-your-openai-key

# Optional: override models (defaults: gpt-4o-mini for sanitize, gpt-4o for main task)
# FAST_MODEL=gpt-4o-mini
# HEAVY_MODEL=gpt-4o
